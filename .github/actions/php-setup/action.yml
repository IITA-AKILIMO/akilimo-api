name: PHP Setup
description: Set up PHP and optionally install Composer dependencies with caching

inputs:
  php-version:
    description: PHP version to install
    default: '8.4'
  coverage:
    description: Coverage driver (none, xdebug, pcov)
    default: none
  install-deps:
    description: Whether to install Composer dependencies
    default: 'true'

runs:
  using: composite
  steps:
    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ inputs.php-version }}
        extensions: mbstring, bcmath, pdo, pdo_mysql, pdo_pgsql
        coverage: ${{ inputs.coverage }}

    - name: Cache Composer dependencies
      if: inputs.install-deps == 'true'
      uses: actions/cache@v5
      with:
        path: vendor
        key: composer-${{ hashFiles('composer.lock') }}
        restore-keys: composer-

    - name: Install Composer dependencies
      if: inputs.install-deps == 'true'
      shell: bash
      run: composer install --no-progress --no-suggest --prefer-dist --no-scripts
